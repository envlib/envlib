# envlib — Open Work

Tracks **verification debts** (work done but not yet proven; each has a close-check — remove the item once it passes) and **backlog** (known work not yet started). The project spans repos — items are tagged: `[envlib]` (this repo), `[ebooklet]`, `[tethys]` (migration checks), `[infra]`. Completed items move to the **Done** section at the bottom (newest first), so Backlog shows only open work.

_Last updated: 2026-07-13 (**ebooklet 0.10.0 (Phases 1+2), envlib 0.1.1, and cfdb 0.9.2 ALL RELEASED to PyPI 2026-07-13** — the full assessment-roadmap stack is shipped. ebooklet 0.10.0: — Phase 1 `99ad450`, Phase 2 committed `ce25694`; wheel+sdist confirmed live, requires_dist booklet>=0.12.7 + s3func>=0.9.3 + msgspec + portalocker. **Post-release: envlib + cfdb locks refreshed to the released 0.10.0** (`uv sync --refresh-package ebooklet`, in-venv PushResult asserts); envlib **137/137** + cfdb **203/203** local green on the RELEASED stack (live tiers attested pre-release on identical code via the overlays). Phase-2 round record: plan dual-blind reviewed on the shared `planning/phase2-design-brief.md` (Gemini via Mike's agy + fresh-context Opus 4.8 subagent, blind held, audits clean, reports preserved as `planning/phase2-review-{claude,gemini}.md`); both verdicts "implementable with changes", zero CRITICALs; all four single-sourced MAJORs author-verified before adoption (F1 `__contains__`-based KeyError guard; F2 PushResult.updated boundary inference; F3 clear() persist-AFTER-truncate; Gemini's remote.py:444 → RemoteMissingError). Mike's synthesis rulings: remote.py:449 target-exists stays builtin ValueError; failures values = `'ClassName: message'` strings; clear() drops unpushed writes silently; offline member-opens document-only. Delivered in ebooklet: typed exception taxonomy (`errors.py`: `Error` base; ReadOnly/UUIDMismatch/RemoteMissing typed with dual ValueError parentage; LockLostError deliberately NOT HTTPError; OfflineError; full raise sweep incl. remote.py); MutableMapping pass (del-of-missing → KeyError; dict-style `update()`; **TRUE `clear()`** — journaled deletes, push-applied, discard-cancellable; `force_shutdown` removed; `Change.update` → `build_changelog`; push-on-reader now ReadOnlyError not AttributeError); `push()` → **PushResult** (updated/failures/`__bool__`; the legacy truthy-partial-dict footgun fixed); **offline read mode** (`offline=False|'auto'|True`, flag='r' only, transport-only 'auto' classifier with typed-errors-first, OfflineSession stub, bulk-read pre-check, `.offline` property); `docs/ops.md` + changelog. Downstream migrated in the same sweep: envlib catalogue.py (typed dispatch — **fixes the latent UnsupportedFormatError-swallow bug**, with a regression test; `open_rcg(offline='auto')` retiring `_read_cached_index` + `_OFFLINE_ERRORS`; `PushResult.failures` in `_raise_on_push_failure`; the previously-unwrapped `register()` push now wrapped; pin → `>=0.10.0`) and cfdb (edataset PushResult docstring; 9 test-assert migrations; pins ×3 → `>=0.10.0`; changelog 0.9.2-unreleased). **Verification**: 10/10 pre-fix proofs on the pristine `99ad450` tree (incl. the envlib swallow bug live on the released 0.9.5/0.1.1 stack); ebooklet **154/154 incl. live** (full parallel run 153/154 → the one failure was the legacy clear-semantics live test, migrated to true-clear + discard, its file re-run 25/25); cfdb **217/217** (203 local + 14 live) + envlib **143/143** (137 local + 6 live) on `--with` overlays with PushResult-presence asserts (version string alone can't distinguish Phase-1/Phase-2 builds — the uv stale-cache trap); ruff F/E9 clean on all introduced code (pre-existing findings in untouched lines left alone; `envlib/tests/conftest.py` has a pre-existing black-format drift, not from this round). Released stack: booklet 0.12.7, s3func 0.9.3, ebooklet 0.10.0, cfdb 0.9.2, envlib 0.1.2 — all coherent on PyPI. **envlib 0.1.2 (the baked commons URL) RELEASED 2026-07-13** and verified by a pristine fresh-install-from-PyPI bare `Catalogue()` against the live commons. Next (2026-07-14): NO v1 upgrades needed (Mike — no pre-launch datasets exist); first commons fill = static-first, fresh-first (see Backlog), then the tethys migration._

_Phase-1 record (2026-07-13, superseded header kept for context): **assessment Phase 1 IMPLEMENTATION COMPLETE — all stages verified; Stage 3+4 diff since committed as `99ad450`**. Shipped this round: Stage −1 ebooklet **0.9.6** (F-1 delete-then-set data-loss hotfix; committed, NOT on PyPI — PyPI still has 0.9.5 with the live F-1 bug); Stage 0 booklet **0.12.7** + Stage 1 s3func **0.9.3** RELEASED to PyPI; Stage 2 (journal + Seam-1, committed `a3d73eb`) and Stages 3+4 (generational format 2 + fsck, uncommitted) in the ebooklet tree as 0.10.0-unreleased. Final attestation: ebooklet **138/138 incl. live** (now parallel: pytest-xdist `-n 4 --dist loadfile`, 8:20 vs 20:32 sequential); cfdb **217/217** + envlib **143/143** on the format-2 overlay. Released stack: booklet 0.12.7, s3func 0.9.3, ebooklet 0.9.5 (PyPI), cfdb 0.9.1. envlib 0.1.1 staged (pins ebooklet>=0.9.6; 0.10.0 satisfies it). **Rulings (Mike, 2026-07-12/13): Phases 1+2 → ONE release 0.10.0 before the commons launch; F-1 = hotfix-first; break-age default 2h; ebooklet serialization → msgspec; pytest-xdist adopted, NO test-file restructuring yet (revisit if one file dominates — likely test_push_integrity).** The three 0.9.5 residuals are closed in-tree: crashed-writer lock orphans (age-gated force_lock), whole-remote-absence (clean absence in `_resolve_missing`), copy_remote orphan replication (manifest-driven). Next: Mike reviews/commits Stage 3+4 → Phase 2 (API/exception pass, offline mode, PushResult + cfdb/envlib isinstance-dict migration; **plan draft + recon + restart package: `~/git/ebooklet/planning/phase2-planning-notes.md`**; dual-blind review confirmed; 0.10.0 releases only after Phase 2, changelog date kept) → 0.10.0 release → public RCG + envlib 0.1.1 → tethys migration.)_

## Verification debts

*(none — the three debts from the 2026-07-02 plan revision were all closed 2026-07-02: tethys interval-start timestamps confirmed at source + live public data; RCG concurrent-writer semantics verified safe-under-lock via live two-writer tests, with failure modes documented in the plan; pandas freqstr instability confirmed empirically against 2.1.4/2.2.3/3.0.3. Evidence in the plan's updated sections and the session report.)*

## Backlog

- [ ] **[tethys] Embedded B2 credentials in extraction-repo configs** (found 2026-07-05 during the variable sweep; **LOW priority**): `tethys-extraction-*` `parameters*.yml` files carry `aws_access_key_id`/`aws_secret_access_key` pairs in tracked git files (seen in era5; likely pattern across repos). All these repos are **private and single-user (Mike-only access)**, so no third-party exposure and no shared-history leak — external risk is minimal. Residual: the era5 key entered this Claude session's transcript (config was read early on), and possibly prior tethys sessions; the repos were NOT in the Gemini review scope. So IF the keys are still live (old 2021–2022 configs — may already be dead), there's a minor argument to rotate in the B2 console; otherwise a someday-cleanup to move creds out of tracked configs. Mike's call; does not block anything.
- [ ] **[tethys] Build the tethys → envlib migration pipeline** (added 2026-07-08; the next major project phase after public-RCG hosting): walk the tethys production datasets into envlib form — apply the plan's migration notes (feature/variable renames, frequency + utc_offset conversion, product_code decomposition, interval-start timestamps), re-derive ids via `Metadata`, build cfdb files, publish into the commons catalogue. Scope/tooling (where the pipeline lives, per-repo vs central) to be planned when picked up; `docs/guide/tethys-migration.md` + the plan's migration-notes section are the specs.
- [ ] **[infra] MkDocs 2.0 is unlicensed — stack docs stay on mkdocs 1.x for now** (investigated 2026-07-08; LOW urgency): the "Currently unlicensed – unsuitable for production use" banner in docs builds is mkdocs-material *warning about MkDocs 2.0* (released without a license per squidfunk's 2026-02-18 analysis), not about material itself. The stack's builds are clean today: mkdocs 1.6.1 (BSD) + material 9.7.6 (MIT), and material caps `mkdocs<2` so unpinned syncs can't pull 2.0. Longer-term watch: squidfunk is steering users toward **Zensical** (their successor generator). **Assessed 2026-07-08 with Mike — do NOT switch yet**: Zensical is pre-1.0 (v0.0.47 as of 2026-07-05) and, while mkdocs.yml + Python-Markdown extensions (pymdownx incl. snippets) carry over, **MkDocs plugins do not yet — mkdocstrings is unsupported**, which would kill every stack site's API reference pages (3 in envlib, ~10 in cfdb). **Revisit trigger**: Zensical ships mkdocstrings-equivalent API-reference support (their roadmap Phase 2/3 "popular third-party plugins"); then migrate the whole stack in one pass. Until then: mkdocs 1.6.x (BSD) + material 9.x (MIT, caps mkdocs<2) is licensed, working, and safe.
- [ ] **[ebooklet] First live fsck proof** (the surviving half of the v1-upgrade item; its upgrade half CLOSED 2026-07-14 — Mike confirms NO v1 remotes need upgrading: he deliberately avoided creating datasets pre-launch for exactly this compatibility reason): run `ebooklet.fsck()` report-mode against a real production remote once (e.g. the commons catalogue or the first published dataset, right after the first real publishes) — hermetic + live-suite covered, never yet run against production. Natural home: the first-datasets round below. Least-privilege option: Mike's read-only key (see the `project_commons_rcg` memory).
- [ ] **[envlib+ingest] esa-sst ingest round IN FLIGHT (2026-07-14) — everything staged, waiting on MIKE'S PUSH**: the first commons dataset (ESA SST CCI v3 OSTIA L4 daily, NZ/Tasman/Southern-Ocean subset 120-200E/60-10S, 1980-06-01→2025-12-31, identity: owner=esa, product_code=cci-sst-l4-v3-nz, variable=temperature/feature=ocean, method=simulation, processing_level=quality_controlled, mean/day/+00:00/0.05deg, version '1', license CC-BY-4.0, doi=CDS 10.24381/cds.cf608234; **dataset_version_id=982d2f9e0767df23b5fa5d3a**, standard_name auto→sea_water_temperature). DONE: repo `~/git/envlib-repos/ingest/envlib-ingest-esa-sst` scaffolded (pyproject released-stack-only, README, PROVENANCE.md with verbatim CC-BY license + 3 DOIs + channel, config.py); build_sst_cfdb.py MOVED from wrf-model-eval (updated: emits envlib-ready cfdbs; staged deletion + pointer notes in wrf-model-eval CLAUDE.md/docs/params.py await Mike's review there); publish artifact `~/data/esa/sst/envlib_sst_cci_v3_nz.cfdb` (20.0GB) repacked from the legacy research build (which stays untouched at cci_sst_v3_nz.cfdb) with the **4-gate fidelity proof PASSED** (coords exact; 26 chunks/var byte-equal vs legacy NaN-aware; 3 full days vs source netCDF, NaN masks exact; envlib validate green); publish.py --dry-run OK. REMAINING: (1) Mike runs `cd ~/git/envlib-repos/ingest/envlib-ingest-esa-sst && uv run python scripts/publish.py` (~20GB, hours; member db_key datasets/cci-sst-l4-v3-nz, db_url https://b2.envlib.xyz/file/envlib/datasets/cci-sst-l4-v3-nz, num_groups=149, creds ~/.envlib/commons.toml); (2) then run `scripts/verify.py` (bare-Catalogue query, antimeridian spatial checks, credential-less open + byte-equality vs local artifact, ice-mask demo, FIRST LIVE FSCK over member+catalogue — closes the fsck Backlog item) AND re-run `scripts/repack_for_envlib.py --fidelity-only` on the cfdb-0.9.3 lock (deferred from the 0.9.3 round: must wait for the push to release the artifact's exclusive booklet lock — a mid-push attempt just blocks on LOCK_SH); (3) bookkeeping: this item + the fsck item + 'First commons datasets' progress → Done records; cross-project line; commons memory. All ingest-repo files are new/untracked (Mike commits when happy). Known wart worked around in the scripts: the cfdb chained-.loc bug (own Backlog item, fix round next after this lands).
- [ ] **[envlib] First commons datasets — static-first, fresh-first (Mike's direction, 2026-07-14; dataset choice OPEN)**: fill the commons starting with STATIC datasets (not continuously updated — no update pipeline needed, defers the quasi-static/back-fill machinery), and preferably first a dataset NOT already in tethys — so the first real publish exercises envlib's native producer flow (metadata authoring from scratch, CV coverage, license/attribution) rather than the tethys migration adapter, and the first commons entry is new value rather than duplicated data. Candidate OPEN again (2026-07-14: Mike parked the precip product as the FIRST dataset — the NIWA licensing complication makes it a poor opener; 'I don't want a complicated first dataset'; he is choosing another). One-question filter for candidates, learned from the precip round: can the license/provenance file be produced TODAY? PARKED LATER CANDIDATE — the `~/git/monitoring-data-processing` NZ precipitation product (ts_ortho; hourly + daily; climate-combined provenance level; ~4.2k stations, records to 1852; frozen June-2026 snapshot). Ownership ruling direction: owner = the curator (GHCN model — the merge/QC/combine are editorial acts), sources honored via CC-BY attribution. **LICENSING GATE (found 2026-07-14)**: FENZ + MetService = CC-BY-4.0 (Mike). NIWA (~46% of station-rows, the authority source): the CliFlo EULA at `cliflo.niwa.co.nz/doc/terms.html` was the SAME restrictive text 2007→2023 per the Wayback CDX record (2-year non-transferable licence; cl 2.4 prohibits hosting in any non-NIWA-website database without express written permission via the GM of Environmental Information) — NOT open-access at any archived point; archived copies saved in the session scratchpad. Resolution paths: (a) verify the ACTUAL acquisition channel per NIWA piece (the `~/data/NIWA` SQLite provenance; niwa-sos vs niwa-cliflo tethys buckets; NIWA's data-catalogue/NZGOAL CC-BY portals), (b) request cl-2.4 written permission, (c) fallback NIWA-free variant (~3.0k of 5.6k station-rows, councils+FENZ+MetService, publishable now). NOTE the same EULA question bears on the existing public tethys niwa-cliflo bucket. Path for the parked precip candidate when picked up: the CC-BY-only variant (NIWA/CliFlo excluded — permission ruled out by Mike 2026-07-14: NIWA's direction of travel is MORE closed; requires a pipeline re-run in monitoring-data-processing with NIWA sources excluded, ~3.0k of 5.6k station-rows retained). Ingest-template requirement adopted from this round: a per-source PROVENANCE RECORD in every ingest repo (license text or written confirmation + acquisition date + channel); for councils, the CC-BY notice usually lives on their data-access pages — capture it at acquisition time. The tethys migration follows once the native flow has shaken out.
- [ ] **[MEGA] File bug reports** (reproducers ready in `s3func/benchmarks/`): (1) conditional writes (`If-None-Match:*`) accepted but NOT atomic under concurrency — 8 simultaneous creates all succeed (`conditional_write_probe.py`); (2) multi-object-delete POST hangs indefinitely on keys containing `$` (singular DELETE works). Both verified live 2026-07-02/03 on `s3.ca-west-1.s4.mega.io`. Mike's call whether/when to file.
- [ ] **[ebooklet] Build out the mkdocs site** (Mike, 2026-07-13, explicitly NOT now): ebooklet has outgrown its two-page site (About + API reference) — 0.10.0 adds the ops guide, offline mode, the typed taxonomy, format 2, fsck. Some post-0.10.0 round: proper guide pages (concepts/format/ops), mirroring the envlib docs structure. The planning/assessment docs deliberately live OUTSIDE the mkdocs source (`planning/` at the repo root, moved there 2026-07-13) so they never publish.
- [ ] **[ebooklet] Push pipelining + progress (booklet 0.12.8 + ebooklet 0.10.1) — IMPLEMENTED + VERIFIED 2026-07-15, awaiting MIKE'S review/commit/release**: the round that fixes the measured one-group-per-~105s push metronome (pack reads were random ~157KB HDD reads under booklet's lock → ~9 reads/s aggregate) and the zero-feedback pushes. **Design evolved during planning** (Mike's capture-in-the-changelog-sweep idea replaced the standalone bulk accessor + queue topology) **and was dual-blind reviewed pre-implementation** (shared brief + both reports in `~/git/ebooklet/planning/push-pipeline-{design-brief,review-claude,review-gemini}.md`; both verdicts "implementable with changes"; convergent CRITICAL on the pulled-bytes RAM/stale-read defect → fixed with the pulled_keys locked-read rule, which also made the design simpler; Claude-only guard-window + per-push-gate + abort-detail findings folded in). Delivered — **booklet 0.12.8**: header-only `locations()` iterator + `compaction_count` property, 12 new tests, suite 152/152. **ebooklet 0.10.1**: loc_map capture inside `create_changelog` (zero extra IO; skew-normalized ts refreshed in-map; comp0 snapshot at capture); phase-B workers read via private fd in ascending captured-offset order (no booklet lock in the hot path); pulled/post-capture members via locked page-cache-hot reads (pulled newer values structurally beat stale offsets); `push_packers` read-gate kwarg (default 1, validated ≥1, flows through cfdb `open_edataset(**kwargs)` / envlib `publish(**open_kwargs)` with zero downstream code changes); staged index ts ≡ packed ts by construction; `PushInProgressError` (prune/clear during push) + `ConcurrentCompactionError` belt (aborts BEFORE commit; nothing staged/journal-cleared; orphans fsck-sweepable); `ebooklet.push` INFO logger (exact upfront totals, per-group pack+PUT seconds, cumulative rate, ETA, summary, commit record); Windows mandatory-lock per-group fallback; changelog + docs/ops.md "Monitoring a push"; pins booklet>=0.12.8 (pyproject + the stale requirements.txt). **Verification**: ebooklet **164/164 incl. live tier** (xdist, 9:55) on the local-booklet overlay — 10 new pipeline tests (content-identical entry-set push, pull-precedence regression, caplog record counts with byte-exact totals, threaded prune-during-push + direct-flag guards, out-of-band-compaction abort with byte-unchanged db object + converging retry, pack/upload failure injection, locked-fallback path warned once, kwarg plumbing both factories); **cold-cache benchmark on the real ~/data array** (discovered to be a 2-spindle RAID0): NEW pack 20.3/26.3/32.0/41.7 MB/s at widths 1/2/3/10 vs OLD 11.9-12.7 MB/s (lock-serialized regardless of threads) — even width 1 ≫ any plausible uplink, so **default 1 confirmed** (RAID0 rewards wider; the per-group pack/PUT log timings are the tuning evidence for any future default change); changelog sweep is a wash as reviewed (17.8s header-only vs 17.5s old full-read on 4GB — seek-dominated). Riders staged: `ebooklet.push` logging config in ingest `scripts/publish.py`; "watching a large push" snippet in envlib `docs/guide/publishing.md`. **ROUND EXPANDED 2026-07-15 by the pre-release E2E test (Mike's call: real 2GB cfdb subset pushed to the test bucket BEFORE release) — it caught FOUR defects the suites/benchmark/design-review all missed**: (a) **pack_group quadratic bytes-concat** (since 0.10.0; ~100GB memcpy ≈ 60-100s CPU per 134MB group — the PRIMARY cause of the production one-group-per-105s cadence, not disk; fixed → bytearray + complexity tripwire test); (b) **s3func timeout was a TOTAL deadline mislabeled as read_timeout** (`Timeout(x)` sets total=; concurrent big PUTs at uplink/N spuriously killed mid-body + full-body retries; ALSO hits slow consumers' large GETs on 0.10.0 today; fixed → `Timeout(connect=30, read=x)`, ships as **s3func 0.9.4** joining the round per Mike; ebooklet pins s3func>=0.9.4; s3func suite 82/83 green); (c) **transport raises escaped the worker error contract** (MaxRetryError crashed the whole push instead of PushResult.failures; fixed at all 3 sites — group workers, pull loop, legacy per-key — verified live: 12 clean per-group failures, no crash, hermetic 105/105); (d) **OPEN — Python-side send stalls at 10 concurrent ~78MB PUTs**: sockets stall >30s mid-body (12/23 groups failed run C) while probes prove per-stream ~2.5MB/s TCP/RTT-limited scaling cleanly to 10.2MB/s at 4×20MB, and an **rclone control (identical 10×78MB single-part shape, same bucket/link) ran 9.3MB/s with zero errors** — environment exonerated, defect isolated to the python http.client/urllib3/ssl sending shape (never seen before because (a) meant ebooklet never truly ran concurrent PUTs). **(d) RESOLVED 2026-07-15 via the dual-blind transport review + fix + gate (per Mike's tripwire ruling)**: both reviewers (Claude arm: 11 experiments incl. ss -ti socket telemetry; Gemini arm: live A/B probes) **independently converged on the identical mechanism AND the identical fix** - a bytes body is transmitted as ONE SSLSocket.sendall whose deadline (urllib3 applies the CONNECT timeout during body send) is fixed at entry and never extended by progress, so any PUT with transmission time > the wall dies deterministically on a healthy socket (Gemini: 10x78MB raw bytes 0/10 vs BytesIO 10/10; Claude: failure time tracks the timeout knob exactly - 5.84s@5s/12.35s@12s - with sockets provably healthy at abort). The GIL-burst hypothesis: refuted by both. **Fix shipped in-tree (s3func 0.9.4)**: put_object (S3 + B2-native) wraps bytes bodies >1MiB (utils.stream_body_threshold) in io.BytesIO -> urllib3 streams 16KiB chunks -> per-chunk idle semantics, retries rewind, payload hash still computed from the bytes in one pass (known cost: one transient payload copy per in-flight upload); connect_timeout param exposed on session()/S3Session; the earlier Timeout-split comment/changelog CORRECTED per Claude's MAJOR (shipped alone it had TIGHTENED the send wall 60s->30s - the two fixes must ship together); NOTE-5 symmetry guard on the group-upload future.result(). Review artifacts: planning/transport-review-{brief,claude,gemini}.md + planning/transport-evidence/. **Tests delivered**: offline send-wall tripwire in the NORMAL s3func suite (throttled loopback sink; FAIL-before proven - the monolithic path dies at exactly the 2s wall - and the streamed path passes in ~5s) + a bytes-wrap threshold unit; the **scale pytest tier** wired in BOTH repos (markers + addopts -m "not scale" + testpaths; NEVER CI) with T2 (s3func: calibrated live send-wall repro, link-speed independent - PASSED live, 61s) and T3 (ebooklet: 12x40MiB real push with the ZERO-urllib3-retry assert that makes run-B-style silent full-body-retry storms a test FAILURE - PASSED live, 8:43 on a slow-link hour, 0 retries). **Suites**: s3func 84/85 incl. live tiers; ebooklet hermetic re-green. **THE RELEASE GATE PASSED 2026-07-15**: the 2GB real-cfdb E2E clean end-to-end - full push 23/23, 1848.9MB in 4:16 at 7.20MB/s mean (peaks 10.4), ZERO failures, ZERO retries, commit OK, both variables byte-equal via fresh-reader pulls, incremental push OK, test remote cleaned; vs 1.25MB/s with retry storms at the round's start. Operational nuance discovered: rewriting one full DAY touches ~460 spatial chunks -> every group repacks (hash-grouping makes spatially-complete updates full re-pushes; localized writes stay cheap). REMAINING: (1) Mike reviews all THREE trees (booklet, s3func, ebooklet - venv overlays: the ebooklet venv runs local booklet+s3func editables; a bare uv sync breaks until deps ship), releases **booklet 0.12.8 + s3func 0.9.4 first**, then ebooklet lock refresh + re-suite -> **ebooklet 0.10.1**, PyPI-presence verification; (2) downstream lock refreshes + suites (cfdb, envlib); (3) Done move + cross-project line + commit planning artifacts. Deferred per the review: retry-backoff scaling for large bodies (MINOR-3), optional uploader gate ~4 for RAM/fairness (MINOR-4), upstream urllib3 report of the connect-timeout-governs-send interaction (candidate). Out of scope per rulings: progress= callback, streaming/multipart PUTs, adaptive gate heuristics.
- [ ] **[s3func] Remove the B2-native module (the promised 1.0 removal; Mike's direction 2026-07-15 — "excessive to manage both APIs")**: `B2Session`/`B2Lock` already carry DeprecationWarnings and have ZERO production usage in the stack (grep-verified: ebooklet/cfdb/envlib are pure S3-compat; only two ebooklet test files import B2Session). The 0.10.1 round demonstrated the dual-surface cost concretely (every transport fix ×2; the BytesIO wrap needed a b2-specific retry-rewind companion fix). Removal round: delete `b2.py`'s native session + `B2Lock` + the `locking.py` registry entry + `__init__` exports + the native lock tests; clean the two ebooklet test imports; changelog + README. Until then the module is frozen-deprecated (the 0.9.4 rewind fix keeps it correct). Rides whenever — its own small diff, NOT part of the 0.9.4/0.10.1 release train.
- [ ] **[s3func] Drop the orjson dependency for stdlib json** (ruled by Mike 2026-07-12 during the Phase-1 serialization decision; NOT part of the 0.9.3 round): all s3func orjson uses are small decode-only response/error parsing (b2 auth, error bodies) where performance is irrelevant — stdlib json removes a compiled dependency. Some future s3func round.

## Done (newest first)

- [x] ~~**[cfdb] Chained `ds[var].loc[...]` ReferenceError — fixed, released as cfdb 0.9.3**~~ — **DONE 2026-07-14** (Mike's ruling: round closed; the deferred `--fidelity-only` re-run moved to the SST item's post-push verification — it exercises the bound form anyway, so it re-proves the artifact, not this fix). Found by the first arm's-length consumer (the esa-sst ingest fidelity gate). Root cause (confirmed by experiment; single-Gemini design review per Mike's ruling): the loc indexer was created once in `Variable.__init__` holding only `weakref.proxy(variable)`; the weak-cached temporary in `ds[var].loc[...]` is freed at refcount-zero mid-expression (deterministic; views worse — never cached at all). Fix: `Variable.loc` is a per-access property returning a strong-ref `LocationIndexer` — keeps chained temporaries alive for the expression, no stored cycle (Gemini empirically showed the eager-strong-ref alternative delays the dataset finalizer to gc passes → file locks persist on 3.12+; refcount-zero collection verified unchanged with gc disabled). Delivered: fix (`indexers.py` + `support_classes.py`), regression `test_chained_loc` (chained read / view read / chained write / notebook chain / bound guard) + chained assert in the live edataset test, **FAIL-on-0.9.2 proof** (all 4 chained forms ReferenceError on the released package, version-asserted overlay), **cfdb suite 218/218 incl. live**, changelog, CLAUDE.md/GEMINI.md weakref rule rewritten (LocationIndexer moved to a documented per-access-strong-ref pattern so no future session reverts it). Released: Mike committed `518bb31` + published; PyPI wheel+sdist confirmed; envlib lock → 0.9.3 (fix feature-asserted in-venv) + **envlib suite 144/144**; ingest repo lock refreshed + bind-first comments made past-tense. Repro: scratchpad `chained_loc_repro.py`. Residuals live elsewhere: fidelity re-run → SST item; cfdb changelog date-stamp edit uncommitted in Mike's tree (rides a later commit). Gemini side-notes, no action: `open_dataset(p).create.coord` ReferenceError is BY DESIGN; `legacy/` is dead code; `v.loc is v.loc` now False (changelogged).
- [x] ~~**[infra] Public RCG hosting — the envlib commons is LIVE and publicly consumable**~~ — **DONE 2026-07-13** (envlib 0.1.2 released with the baked URL; final verification = pristine `uv run --isolated --with envlib==0.1.2` fresh install from PyPI → bare `Catalogue()` → the live commons, credential-less). B2 lifecycle "keep only the last version": Mike confirms set. **[infra] Public RCG hosting — HOSTED + VALIDATED 2026-07-13; URL baked, staged for envlib 0.1.2 (move to Done when 0.1.2 ships)**: the commons RCG is live on the public B2 bucket at `https://b2.envlib.xyz/file/envlib/envlib-commons/catalogue` (custom domain fronting the bucket, so the baked URL outlives bucket moves; created flag='n' num_groups=13 on format 2, empty). Validated credential-less E2E on RELEASED code: raw 200 + ranged 206 through the domain; ebooklet 0.10.0 public read (type + uuid match); envlib 0.1.1 bare `Catalogue()` via the env var. Staged in-tree for 0.1.2: `PUBLIC_RCG_URL` baked (env var still overrides), docs note updated, guard tests patched, version 0.1.2 + changelog. **Full pipeline dress rehearsal PASSED on the live commons 2026-07-13** (pre-migration, Mike's request): publish (real cfdb member on the public bucket + commons entry) → bare `Catalogue()` query via the baked URL → credential-less member open + data-value verification through Cloudflare → `deregister(delete_data=True)` → commons empty again, uuid unchanged, bucket listing verified clean (no orphans/tickets); the published entry confirmed credential-free (envlib strips keys, stores only the public db_url). Residual for Mike: confirm the B2 lifecycle rule is "keep only the last version" (ebooklet GC/deletes otherwise leave old versions publicly readable and accumulating) — unverifiable from here without B2 account API access. Original record: read-only public-HTTPS RCG on Backblaze (S3-compatible), `ENVLIB_PUBLIC_RCG_URL` env-var override.
- [x] ~~**[envlib] Release 0.1.1 — the ebooklet-0.10.0 companion**~~ — **DONE 2026-07-13** (Mike merged dev→main PR #3, tagged + GitHub release, published locally; PyPI 0.1.1 wheel+sdist confirmed live with requires_dist ebooklet>=0.10.0 + cfdb>=0.9.1 + booklet>=0.12.6). **cfdb 0.9.2 released the same day** (PushResult docstring + ebooklet>=0.10.0 pins + version bump, no code changes; PyPI confirmed). envlib's cfdb pin deliberately stays >=0.9.1 — the honest functional floor (dataset_type property); 0.9.2 adds nothing envlib needs (Mike's call 2026-07-13). Post-release: envlib lock refreshed to the fully-released stack (cfdb 0.9.2 + ebooklet 0.10.0), **137/137 local re-attested**; live tier **6/6** ran pre-release against the released ebooklet 0.10.0. Release-process note: the GitHub Build workflows are deliberately disabled across the stack (Mike publishes locally for timing control) — a tag/GitHub-release existing does NOT mean shipped; the first 0.1.1 upload slipped and was caught by PyPI verification (now a standing memory rule). Original record: **[envlib] Release 0.1.1 — now the ebooklet-0.10.0 companion** — **extended 2026-07-13 (Phase-2 sweep; see the header): typed exception dispatch (UnsupportedFormatError-swallow bug FIXED with regression test), `open_rcg(offline='auto')` replacing `_read_cached_index`, `PushResult.failures` migration, `register()` push wrapped, pin → `ebooklet>=0.10.0`, changelog 0.1.1 section rewritten; 143/143 incl. live on the 0.10.0 overlay; lock refreshed to the RELEASED 0.10.0 2026-07-13 (137/137 local re-attested).** Original record — **implemented + verified 2026-07-09 (uncommitted; rides Mike's next envlib release, no hard ordering — pre-0.1.1 behavior on ebooklet 0.9.3 is no worse than today)**: `publish`/`register`/`deregister` now check `push()` returns and raise `RuntimeError` naming failed keys on partial failure (previously silently discarded at 3 sites — Gemini); `refresh()` re-raises `ebooklet.RemoteIntegrityError` instead of swallowing it into the offline-cache fallback (Claude); the broad bootstrap `except ValueError` now distinguishes a uuid mismatch (stale cache vs recreated remote) with an actionable warning; pin `ebooklet>=0.9.3`; changelog + publishing-guide failure-handling docs; 3 new tests (137 local total). **Extended 2026-07-09 (cfdb-0.9.1 staging)**: `_sys_dataset_type()` helper deleted — both call sites use cfdb's new public `dataset_type` property; pin `cfdb>=0.9.1`; changelog line. Verified 137/137 local + 6/6 live on the patched stack. **Extended 2026-07-12 (ebooklet-0.9.5 rider)**: pin `ebooklet>=0.9.3` → `>=0.9.5` (F1 exact-key delete fixes — the group variant is reachable via cfdb chunk deletion) + changelog line; 143/143 on the 0.9.5 overlay; lock refresh once 0.9.5 is on PyPI.
- [x] ~~**[ebooklet] Architecture assessment → the full roadmap (Phases 0+1+2) SHIPPED as ebooklet 0.10.0**~~ — **DONE 2026-07-13** (Mike committed Phase 2 `ce25694`, released; PyPI 0.10.0 wheel+sdist confirmed live; locks refreshed post-release — see the header). Phase 0 = 0.9.5 (own Done entry); Phase 1 = journal + Seam-1 decomposition + generational format 2 + fsck + F5 lock safety (`99ad450`; 138/138 incl. live); Phase 2 = typed exception taxonomy + MutableMapping pass (true clear()) + PushResult + offline read mode + docs/ops.md + the cfdb/envlib migration sweep (`ce25694`; ebooklet 154/154 incl. live, cfdb 217/217 + envlib 143/143 on overlays; round docs `planning/phase2-{design-brief,review-claude,review-gemini}.md`). Original record: **[ebooklet] Architecture assessment — DONE 2026-07-12; roadmap adopted, implementation pending**: dual blind review (Claude Fable 5 + Gemini, same brief, audits clean) delivered convergent verdicts — Seam 1 targeted refactor (explicit remote replacement; num_groups persisted in a reserved local-booklet key — both reviewers independently proposed the same mechanism), Seam 2 REDESIGN before-migration (worse than recorded: cross-session deletes silently lost/resurrected with NO clock skew, read-your-writes violated under skew — fix = persistent pending-change journal in a booklet reserved key, no format change), Seam 3 REDESIGN before-migration (**both models independently designed generation-numbered immutable group objects + single-PUT atomic commit** — embedded manifest per Gemini, format-gating per Claude; kills the mid-push window, the `_metadata` split-brain, and makes fsck trivial). Plus the F1 CRITICAL above, lock-lifecycle gaps (force_lock breaks live writers), a >4GiB group crash, MutableMapping/API/exception-taxonomy pre-1.0 bundles. **Full synthesis + phased roadmap: `~/git/ebooklet/planning/architecture-assessment-synthesis.md`** (Phase 0 — **SHIPPED 2026-07-12 as ebooklet 0.9.5, incl. two review-found absorbs; see Done**; Phase 1 ≈6–9 days before migration — **NEXT**: booklet reserved-key support + journal + Seam-1 decomposition + generational immutable groups + F7 guard + fsck + F5 lock safety; Phase 2 pre-1.0, pulled forward per the ruling below). **Mike's rulings**: (2026-07-12) old-format compatibility dropped entirely — single-format generational implementation, no legacy read path or journal seeding; upgrade = push-then-re-push the few existing remotes; the no-compat window closes at the commons launch. (2026-07-12, post-0.9.5) **Phases 1+2 are separate reviewed/committed rounds but ONE release (0.10.0) before the commons launch** — one format bump + one API break, so the commons RCG is created on the final format/API; cfdb+envlib migrate the `isinstance(result, dict)` push-check in the same sweep. Reports: all in-repo and committed (`planning/architecture-assessment-{task,claude,gemini,synthesis}.md` + the fake_s3 harness). The offline-mode and clock-skew items below are absorbed into this roadmap (Phase 2 / Phase 1 respectively).
- [x] ~~**[ebooklet] no offline fallback for unreachable remotes**~~ — **DONE 2026-07-13 (shipped in 0.10.0, Phase 2)**: `open_ebooklet`/`open_rcg` take `offline=False|'auto'|True` (read-only; 'auto' falls back on transport-level unreachability only; unmaterialized reads raise typed `OfflineError`); envlib's hand-rolled `_read_cached_index` fallback is retired in favor of `open_rcg(..., offline='auto')` (rides envlib 0.1.1). Original record (2026-07-07): `open_*` hard-fails on DNS/connect errors (only 404-with-local-file opens locally) — envlib implements its own cached-index fallback by reading the local booklet file directly.
- [x] ~~**[ebooklet] Timestamp-based changelog silently ignores clock-skewed local edits**~~ — **DONE 2026-07-13 (shipped in 0.10.0, Phase-1 Stage 2)**: the changelog is the union of the timestamp diff and the persistent journal, and skew-stamped journaled edits get their timestamp advanced so they both push AND propagate to readers. Original record (Gemini review finding 6, DEFERRED 2026-07-02): a local modification whose timestamp was OLDER than the remote's never entered the changelog, so it never uploaded; the 0.8.4 warning was the mitigation.
- [x] ~~**[ebooklet] Review + release 0.9.5 — Phase 0 of the assessment roadmap (F1 + 5 items + 2 review absorbs)**~~ — **DONE 2026-07-12** (Mike reviewed, committed `dd1d7ec` incl. the review artifacts, pushed to GitHub, released; PyPI 0.9.5 wheel+sdist confirmed live with requires_dist booklet>=0.12.6 + s3func>=0.9.2). **Post-release: envlib + cfdb locks refreshed to 0.9.5; envlib 143/143 and cfdb 217/217 on the RELEASED stack**; changelog heading dated (one-line uncommitted edit in ebooklet, rides the next commit). Round record: Design dual-blind reviewed pre-implementation (Claude Opus 4.8 xhigh + Gemini, shared brief `planning/phase0-design-brief.md`, both reports preserved in-repo; both: implementable, nothing blocking; audits clean). Delivered: (1) **F1 CRITICAL** — exact-key all-versions group deletes (was string-prefix: emptying group `1` destroyed `10`,`11`,`12`…); bounded `delete_remote` = {exact db_key} + {db_key + '/'} (was bare prefix: wiping `mydb` wiped `mydb2`), **lock namespace deliberately excluded** (bare prefix deleted the 'n'-push's OWN live tickets mid-push — improves on the synthesis line per both reviewers); writable probe relocated inside `db_key + '/'`; (2) F4 — deleted-member reads route through `_resolve_missing` (re-check→clean absence or `RemoteIntegrityError`; push keeps the lost-keys self-heal as the recovery mechanism); (3) `format_version` metadata stamp + `UnsupportedFormatError(ValueError)` too-new refusal; (4) close-time UserWarning on unpushed deletes; (5) README "Data Formats and Stability" + RCG entry v1 declared FROZEN; (6) http-db_url warning at S3Connection (single choke point — Claude traced; Gemini's two-site variant double-fires); (7) **review absorb, Claude MAJOR author-verified**: empty-valued grouped members (`b''`, index length 0) were silently dropped by evict-then-repack — pull loop now materializes them directly; (8) `delete_object` returns error-or-None (failed empty-group delete → retryable partial failure). **Verification**: new hermetic test layer (`tests/fake_s3.py` + 3 test files — first credential-free remote/push tests); 7 regressions FAIL on a 0.9.4 worktree; full suite **95/95 incl. live tier** (22.6 min); 2 new live B2 tests incl. version-level purge proof + own-lock-ticket survival; downstream on the 0.9.5 overlay: **cfdb 217/217 incl. live edataset tier**, **envlib 143/143 (137 local + 6 live)**; ruff F/E9 clean on all changed/new files. Residuals documented in changelog: teardown no longer sweeps crashed-writer lock orphans (`force_lock` until Phase-1 age-gate); pre-existing probe orphans at `db_key + <hex>` unsweepable; `copy_remote` blind listing replicates orphans (Phase-1 manifest copy).
- [x] ~~**[envlib] Live-tier silent-skip on py3.10 venvs**~~ — **RESOLVED 2026-07-12**: envlib's project venv had sat on Python 3.10.0 (switched during the docs-round CI check, never restored), where the conftest's missing-`tomllib` path made the LIVE tier **silently skip** — so the resilience round's envlib "verification" was local+mocked only (corrected; the live tier re-ran green 2026-07-09 on the patched stack and 2026-07-12 on the released stack). conftest now falls back to `tomli` like the other repos' tests, and `tomli; python_version < '3.11'` is in the dev group (lock refreshed post-cfdb-release) — the gate can no longer silently skip on 3.10.

- [x] ~~**[ebooklet] Review + release 0.9.4 — flag='n' second-push data-loss fix**~~ — **DONE 2026-07-12** (Mike reviewed, committed, released; PyPI 0.9.4 confirmed live). Found by cfdb 0.9.1's live tier the first time it ran against ebooklet 0.9.3: `update_remote` wiped the ENTIRE remote on *every* push of a `flag='n'` session (`utils.py:529-530`) and re-uploaded only the current changelog's groups — so a second push (cfdb's documented "push mid-session, push again at close" pattern) destroyed everything the first push uploaded, reporting success. **Pre-existing since the 0.9.0 'n' contract; silent until 0.9.3's missing-object loudness made readers raise `RemoteIntegrityError`** (the old test assertions happened to read only surviving keys — third latent bug surfaced by the resilience round). Fix (Mike's design): the session **downgrades `_flag` 'n'→'w' once the replacement push completes** (wipe happens exactly once; partial-failure pushes do NOT downgrade, so a retry redoes the full wipe-and-replace). All four post-open `_flag` consumers audited (push purge, update_remote arg, `_pull_remote_index` ×2 — post-flip uuid check passes since 'n' publishes the local file's uuid). Diagnosis chain: deterministic repro with per-step S3 object listings → component bisection via PyPI overlays (fails identically on the July-5 stack → not a regression from this week). **Verification**: regression test (multi-group survive + metadata + fresh reader) passes on the fix and FAILS on a pristine 0.9.3 worktree; envlib exposure: none (publish pushes once per session).
- [x] ~~**[cfdb] Review + release 0.9.1 — dataset_type API gaps**~~ — **DONE 2026-07-12** (Mike reviewed, committed, released; PyPI 0.9.1 confirmed live with requires_dist booklet>=0.12.6 + ebooklet>=0.9.4 extra). Both recorded gaps fixed + three more found by the Gemini design review (Mike-driven, solo — findings independently re-verified before adoption; verdict was "not implementable as specified" and it was right): (1) `open_edataset` supports ts_ortho at create and both factories now choose the class from the STORED type for existing datasets (the `dataset_type` param is create-only, documented); (2) public `dataset_type` property on `DatasetBase` (str, all classes + views); (3) **Gemini CRITICAL**: msgspec doesn't coerce on construction, so `_sys_meta.dataset_type` was a str on fresh datasets vs enum on reopened — normalized to the enum at creation (`main.py:765`; on-disk unchanged); (4) **pre-existing latent crash**: creating a coordinate on any REOPENED dataset raised `TypeError` (`'ts_' in <enum>`) — `creation.py` now uses the string property; (5) `combine.py` switched to property comparisons. Also: docstrings/docs (s3-remote ts_ortho section, dataset properties table, edataset `push()` doc), changelog entry, pins `ebooklet>=0.9.4`. **Verification**: full suite **217/217 incl. live tier** (with the ebooklet-0.9.4 overlay); new local regression FAILS on a 0.9.0 worktree (AttributeError); new live ts_ortho E2E (create→push→fresh reopen as `ETimeSeriesOrtho`); envlib downstream 137/137 local + **6/6 live** (ts_ortho publish→query→open) on the patched stack.
- [x] ~~**[ebooklet] Review + release 0.9.3 — 404-integrity re-check protocol**~~ — **DONE 2026-07-09** (Mike reviewed, committed, released; PyPI 0.9.3 confirmed live with requires_dist s3func>=0.9.2 + booklet>=0.12.6). The silent `404: pass` sites (every value-fetch 404 is by construction "index claims it, object missing") now return a truthy `MissingRemoteObject` marker; `_resolve_missing` re-pulls the index ONCE per operation (Mike's re-check-then-loud ruling): fresh index no longer claims → clean absence (stale local values purged; metadata unset via `set_metadata(None)`); still claims → **`RemoteIntegrityError`** (new export, subclasses HTTPError). `Change.pull`'s body extracted to `_pull_remote_index` under a new `_index_lock` (RLock) so the handle swap can't close the index under concurrent same-instance readers (Gemini CRITICAL); re-pull's own raise paths logged-with-context then re-raised (Claude MAJOR); push-side 404s now surface as partial-failure dicts instead of the old lost-keys self-heal (**behavior change, changelog has the recovery recipe**). Pins `s3func>=0.9.2`, `requirements.txt` unstaled, changelog 0.9.3 entry + 0.9.2 pin line. **Dual-blind design review 2026-07-09**: both models found blocking gaps in the original design (thread-safety CRITICAL Gemini; stale-metadata both; envlib-coordination both, different angles; + 4 Claude MAJORs) — all folded in pre-implementation. **Verification**: new `test_404_integrity.py` 9/9 live (integrity raise, legit-deletion quiet paths grouped+per-key, metadata branch, push-side loud-and-retryable with the previously-dropped member surviving, concurrent-reader smoke under the lock, live race test); **full suite 74/74** on patched s3func; envlib 137/137 + cfdb 202/202 local downstream.
- [x] ~~**[s3func] Review + release 0.9.2 — 5xx/429 retry fix + changelog**~~ — **DONE 2026-07-09** (Mike reviewed, committed, released; PyPI 0.9.2 wheel+sdist confirmed live). Root cause of the CI/local B2-incident failures: the single urllib3 `Retry` (shared by ALL three session classes — **correction to the original record: there is NO botocore path in s3func; `retry_mode='adaptive'` was dead code, now docstring-deprecated**) had no `status_forcelist`, so 5xx/429 *responses* were returned un-retried. Fix: `status_forcelist=(429, 500, 502, 503, 504)` + `raise_on_status=False` (exhausted retries RETURN the final response — callers dispatch on `resp.status`; a raise would bypass the lock-PUT check, delete_objects chunk handling, and b2's manual upload loop); POST deliberately excluded (default `allowed_methods`) protecting non-idempotent B2-native POSTs from compounding. **Dual-blind design review 2026-07-09** (Claude Opus 4.8 xhigh + Gemini via agy, blind held; both: implementable as specified; every urllib3 claim empirically confirmed by both). Also: new root `CHANGELOG.md` (backfilled to 0.3.0), README retry claims corrected, docstrings. **Verification**: 6 new tests against a local scripted flaky HTTP server (retry-to-success with request counts, persistent-500 returns the response after exactly N+1 requests, POST sees 1 request, Retry-After honored; the zero-backoff fixture patches `Retry.get_backoff_time` at CLASS level — urllib3 clones per increment); full suite **75 passed / 1 skipped incl. live B2+MEGA tiers**.
- [x] ~~**[ebooklet] Review + release 0.9.2 — post-booklet-0.12.6 tidy-ups + pin bump + changelog**~~ — **DONE 2026-07-09** (Mike reviewed, committed, pushed; PyPI 0.9.2 wheel+sdist confirmed live; CI green after rerunning two transient B2-incident failures — root causes recorded as the `[s3func]` 5xx-retry and `[ebooklet]` index-404 Backlog items). Delivered: (1) stale lock-lifetime comment at `utils.py:539-541` rewritten (single-pass `items()` kept for the perf rationale; the self-deadlock constraint gone as of booklet 0.12.6); (2) the four direct `._file` accesses in the push db-object path (`utils.py:676-695`) now hold the owning booklet's `_thread_lock`, with the `remote_index` seek+read adjacent under one lock; (3) `booklet>=0.12.6` pin + `uv.lock` refresh; (4) root `CHANGELOG.md` (booklet-style; rich entries 0.8.4–0.9.2 from the release records, reconstructed one-liners back to 0.6.0, README pointer). **Verification**: full live suite effectively 65/65 (one transient B2 `InternalError` on a lock PUT, passed on immediate rerun); envlib 134/134 + cfdb 202/202 local green on booklet 0.12.6. Same round: `booklet>=0.12.6` pins staged in the cfdb working tree (rides 0.9.1) and the envlib working tree (rides the next envlib release).
- [x] ~~**[booklet] Review + release 0.12.6 — iterator lock-across-yield deadlock fix + fixed-write integrity guards**~~ — **DONE 2026-07-09** (Mike reviewed, committed, pushed; PyPI 0.12.6 wheel+sdist confirmed live). The original deadlock (`for k in blt.keys(): blt[k]` hangs single-threaded; found 2026-07-07 via a hung envlib test) fixed with the recorded design: per-step locking (`Booklet._iter_locked` — seek+read under `_thread_lock`, lock released before every yield) + a per-instance `_mutation_count` snapshotted at iterator start and checked each step → `RuntimeError('booklet mutated during iteration')` on any layout mutation (dict-style; `set_timestamp` explicitly allowed during iteration). **Dual-blind design review 2026-07-08** (Claude = Opus 4.8 xhigh subagent, Gemini via Mike's `agy`, blind held, post-run file audits clean; both: implementable, zero CRITICAL/MAJOR) after a first adversarial pass **refuted one recorded claim** — the fixed-length *file* iterator was NOT per-step-safe (cursor carried in the shared file position; interleaved reads yielded garbage/livelocked) — its local-position rewrite became part of the fix. Five same-family drive-bys shipped in the round (all Mike-approved): `FixedLengthValue.set()` override (inherited variable framing corrupted fixed files), `_pre_value` value-length validation (wrong-length values silently corrupted the stride), `_set_file_timestamp` now holds the lock, `set_timestamp` normalizes str/datetime (crashed pre-fix), and a `_compaction_count` guard making `prune()`/`clear()` during `map()` raise instead of corrupt. **Verification**: 129/129 booklet (95 existing + 34 new regressions; all 34 verified to FAIL on a 0.12.5 worktree); downstream via verified `uv --with` overlays — ebooklet 65/65 live, envlib 134/134 local, cfdb 202/202 local; iteration overhead 1.13–1.21× (~0.3 µs/key) on 100k keys. Docs: README iteration-semantics section, booklet CLAUDE.md iteration contract, docstrings, and a **new root `CHANGELOG.md`** (backfilled to 0.9.0 from commit history; changelog upkeep is now part of the stack release routine). envlib's `list(blt.keys())` workarounds stay (harmless). Residual pin bumps: staged 2026-07-09 in the ebooklet-0.9.2, cfdb, and envlib working trees (see the `[ebooklet] Review + release 0.9.2` item).
- [x] ~~**[envlib] Post-0.1.0 loose ends**~~ — **CLOSED 2026-07-08, both decisions ruled by Mike**: (1) **B2 test key: accepted-risk, no rotation** — the key is an application key scoped to the `achelous` test bucket only (nothing important in it); the exposure was transcript-only (never git/public), and the pytest fixture repr is masked so it can't recur. (2) **aggregation_statistic map CONFIRMED**: `instantaneous`/`continuous`/`sporadic` → `point`, `cumulative` → `sum` (interval-sum check per series at migration time), `mean` → `mean` — folded into the plan's migration notes; the docs' tethys-migration page already stated it.
- [x] ~~**[tethys] aggregation_statistic migration mapping**~~ — **RESOLVED 2026-07-08** (ruling above; recorded in `plans/architecture_plan.md` migration notes + `docs/guide/tethys-migration.md`).
- [x] ~~**[envlib] First release: 0.1.0 on PyPI**~~ — **DONE 2026-07-08** (Mike committed, pushed, released; docs site live via Pages; post-release gardening done the same day: stale "unreleased" notes removed from README/installation/changelog, Home-page snippet-path fix, `.claude`/`.antigravitycli` gitignored + the stray tracked file removed).
- [x] ~~**[envlib] Documentation pass**~~ — **DONE 2026-07-08** (Mike reviewed + tweaked — consumer-first quickstart, public-commons-first examples, num_groups prime tip — approved for push; CI simulated credential-less: 134 passed / 6 skipped with the exact test.yml command; decision: NO live tests in CI — config-gating stays the stack convention, workflow_dispatch + dedicated key is the shape if ever wanted). Delivered: rewritten README (doubles as the docs landing via the snippet embed); 12 new mkdocs pages — getting-started (installation, quickstart), concepts/data-model (dataset vs version + the three ids + lifecycle), guide (browsing-querying, opening-data, producing-datasets, publishing, vocabularies, tethys-migration), reference (catalogue/metadata/vocabularies via mkdocstrings, replacing the old stub), changelog (0.1.0 unreleased); docstring terminology sweep ("cfdb file" vs "dataset") + a Catalogue usage example; repo-URL fixes (pyproject `Source`/`Documentation`, mkdocs `repo_url`, README badges — all now `envlib/envlib`); mkdocs config fixes (legacy mkdocstrings `import:` → `inventories:`, admonition extension, full nav). All plan-mandated content included (both post-delist deletion paths, back-fill + quasi-static caveats, antimeridian convention, owner semantics, credentials trust warning, etc.). **Verification**: `mkdocs build --strict` clean (py3.10, matching the Pages CI); every local-runnable doc snippet extracted and EXECUTED via a scratchpad harness (6 fences + output-claim assertions — caught one missing numpy import); 134 tests + ruff/black/mypy green after the docstring/annotation touches. Docs auto-deploy to GitHub Pages on Mike's next push to main.

- [x] ~~**[envlib] Implementation steps 1–5**~~ — **DONE 2026-07-07** (all code implemented and verified; close-out items split into the Backlog entry above). Delivered: `envlib/vocabularies/` (8 bundled JSONs — CF v94 5071 names + 599 aliases, ODM2 993 terms ∪ 10 flagged extensions with the full curation table from `plans/variable_inventory.md` encoded and unit-enriched, ENVO URIs looked up via OLS4; `list`/`canonical`/`is_valid`/`get_cf_standard_names` tri-state/`refresh` with user-dir overlay), `envlib/metadata.py` (validators, hash core, `compute_station_id`, `Metadata` with lazy utc_offset reduction, `from_attrs(validate_cv=)` drift tolerance), `envlib/catalogue.py` (Catalogue/DatasetRef, query with antimeridian+great-circle+latest-per-series semantics, validate, publish/register/deregister with shared-target guard, booklet-direct offline fallback, public-RCG placeholder), full exports. **Verification**: 132 local + 6 live tests green (grid + ts_ortho publish→query→open E2E on B2, no-op idempotence, modified_at semantics, retraction with verified deletion, offline fallback; bucket swept clean, verified independently); ruff/black/mypy clean; golden vectors cross-checked by an independent stdlib reimplementation before pinning. **Dual-blind identity-core review DONE 2026-07-07** (Claude Opus subagent + Gemini via Mike's agy, blind protocol held, both re-derived all golden vectors byte-for-byte): both found the `__eq__`/`__hash__` reduction inconsistency (fixed); Gemini's empty-Point crash + Claude's NaN-hash finding fixed together (reject with ValidationError); error contract unified (`ValidationError(ValueError)`); Mike's rulings: slugs must contain ≥1 alphanumeric (tightened), phase-coincident utc_offsets stay distinct (documented in plan). Notable during the round: process failure recorded in memory (checkpoint handoff not delivered — see `feedback_checkpoint_stop_and_answer`); booklet keys()-lock deadlock found via a hung test (own Backlog item); plan example sweep (`variable='air_temperature'` → `'temperature'`, stale pre-curation examples). **Naming ruling (2026-07-07, post-checkpoint, renamed throughout code+plan the same day)**: *dataset = the version-spanning identity* — `dataset_id` is now the 10-field hash (tethys-like; the query default "latest version of each matching dataset" groups by it) and `dataset_version_id` the 11-field hash (catalogue entry key, open/deregister target, `derived_from` referent). Hash constructions and golden-vector values unchanged — only the names swapped; attr keys are `envlib_dataset_id` + `envlib_dataset_version_id`.

- [x] ~~**[envlib] Curate the variable → CF standard_name mapping**~~ — **DONE 2026-07-06** (decision-complete; encoding of `variable.json` folds into implementation step 1, cross-referenced in that item). Full curation in `plans/variable_inventory.md`: 47 tethys parameters swept from 615 production dataset blocks, reconciled against the live ODM2 list (993 terms) and validated against CF v94 (5071 names). Rulings R0–R5 all decided with Mike: **R0 (ocean-only)** — `sea_water_*` names used ONLY when `feature=ocean`, so freshwater temperature/EC/DO/turbidity/phosphate curate to "none applicable"; **R1** — variable CV = ODM2 ∪ flagged envlib extensions; **R2** — clean envlib spellings with `odm2_term` recorded; **R3** — `snow_depth`→`snowfall` (was misnamed WRF `SNOWNC`); **R4** — ammonia params merged; **R5** — naturalised_streamflow/recharge/velocity singles. **Design change**: CF `standard_name` is no longer required to register — envlib DERIVES it from the curated `(variable,feature)` mapping (auto-populate default, user override validated, absent where empty, warn where uncurated); `units` stays user-owned. Architecture-plan edits applied throughout (Data-variable requirements, variable-CV extension policy, ocean-only "none applicable" rationale, feature + variable rename maps, Metadata + vocabulary examples). Adjacent gap spun off as its own `[tethys] aggregation_statistic migration mapping` backlog item. Regenerate the inventory via the scratchpad sweep script if tethys configs change.
- [x] ~~**[envlib] Finalize the frequency_interval code table**~~ — **DONE 2026-07-05, decided with Mike and written into the plan (vocabulary section)**: 12 canonical codes — `1min, 5min, 10min, 15min, 30min, 1h, 3h, 6h, 12h, day, month, year` (+ `None` for irregular). `day` is canonical with a **closed input-alias table** (`24h` → `day`, `60min` → `1h`) — aliases are input-only, never hashed, mirroring the utc_offset `±HH` shorthand; an alias can never later become a distinct cadence. **Admission rule**: fixed sub-daily durations must divide 24 h evenly (unambiguous day-aligned anchoring) — which excludes `week`/`season`/multi-day composites (`8day`) from v1 (no self-evident global anchor → would reintroduce anchoring variants; such datasets use `None` for now); `month`/`year` are the only calendar codes (utc_offset always retained). Validation is a closed-enum lookup — no parser, no pandas. tethys migration mapping recorded in the plan (`T`→`1min`, `10min`→`10min`, `1H`→`1h`, `24H`→`day`); golden-vector list extended with the alias-map cases.
- [x] ~~**[envlib] Resolve flagged plan judgment calls**~~ — **RESOLVED 2026-07-05, Mike's rulings applied to the plan**: (1) hash-order of `processing_level` stays #5 — plan now notes the order is hash-internal only, user-facing display order is free to differ; (2) `deregister()` stays one method + `delete_data` flag — the "delete data after a plain delist" path is now specified (re-register→retract as the recommended guarded path, or direct ebooklet `delete_remote()` with owner credentials, which bypasses the shared-target guard) and user docs must spell out both; (3) `envlib_` attr prefix kept; (4) `modified_at` bumps only on real change — confirmed as-is; (5) placeholder frequency codes → folded into the frequency-table backlog item, carrying the `day`-vs-`24h` single-spelling decision (Mike leans `day`); (6) Metadata example corrected to ECMWF ownership (`owner='ecmwf'`, `license='Copernicus-1.0'`, Copernicus attribution) — mirroring never confers ownership. Original record: judgment calls flagged during the 2026-07-02 plan revision, excluded from the dual-blind review brief as known-open.
- [x] ~~**[envlib] Dual-blind independent review of the architecture plan**~~ — **DONE 2026-07-05, findings applied to the plan the same day.** Claude reviewer (fresh-context Opus 4.8 subagent, xhigh) + Gemini (Mike-driven `agy`; needed a neutral-worded twin brief after Gemini refused the adversarial wording), same scope for both (5 stack repos at their release commits + tethysts/tethys-utils/tethys-data-models), blind protocol held, post-run file audit clean. **Six findings found independently by BOTH models** (spatial_resolution numeric canonicalization gap; utc_offset reduction unenforced + `-00:00` + range validation; hash string→bytes encoding unspecified; station_id WKB byte-order machine-dependent; station_id signed-zero; transform_bounds is a no-op for native-geographic 0–360 sources) **+ two strong singles** (Gemini: deregister `delete_data=True` shared-target destruction trap; Claude: mixed-case CV query mismatch — `license='CC-BY-4.0'` would never match) + migration nuances (pandas `label` default is frequency-dependent; tethys `'12H'`-format utc_offset conversion; tethys hashed polygons too) + doc polish. All synthesized and applied to `plans/architecture_plan.md` with Mike's rulings: spatial_resolution rejects non-canonical spellings (never rewrites); calendar cadences always retain utc_offset; deregister shared-target guard adopted; ts_ortho stays 2D-Points-only in v1 (lines/polygons as a future extension on the same derivation); `point` name kept. The two brief files (adversarial + neutral wording) were deleted after the round closed; this record is the review's summary. **Implementation steps 1–5 are now ungated.**
- [x] ~~**[cfdb] Review + release 0.9.0**~~ — **DONE 2026-07-05** (committed `d8d3e0e`, PyPI 0.9.0 live). Original record: — implemented 2026-07-03 in the working tree (branch `dev`, uncommitted, awaiting Mike's review): both footgun fixes above + Attributes split-brain fix + public `Dataset.sync()` + docs (s3-remote guide corrected — it falsely claimed auto-push on close; changelog; architecture lifecycle) + version bump 0.8.7→0.9.0 + the pin bumps (`booklet>=0.12.4`, 3× `ebooklet>=0.9.0`) + `uv.lock` upgraded s3func 0.8.5→0.9.0 (the old lock reproduced the `!`-key signature failure live — confirms the ebooklet `s3func>=0.9.0` pin matters). Plan was Gemini-design-reviewed pre-implementation (6 findings: 1 CRITICAL refuting my original flush design — accepted+fixed; 4 confirmations; 1 adjustment). Verification: 215/215 full suite green (8 new regressions — all verified to FAIL on pre-fix code, local and live); envlib publish-flow smoke E2E green (mid-session publish → concurrent fresh reader → fresh-local `'w'` attach+append → no-op session pushes nothing); bucket swept clean. After release: tick this + sweep the architecture plan's workaround notes (close→reopen→push, hydrate-first) + update cross-project backlog line.
- [x] ~~**[cfdb] `EDataset.push()` inside a creation/modification session uploads a stale SysMeta**~~ — **RESOLVED in the 0.9.0 working tree 2026-07-03** (see the cfdb review+release item): `push()`/`changes()` now flush all in-memory state via the new public `Dataset.sync()` first. Scope grew during the Gemini design review: the flush required fixing a pre-existing **Attributes split-brain** (weak `_var_cache` → duplicate live `Attributes` dicts for one variable; `close()` flushed the STALE one last = silent data loss in plain local cfdb) — fixed via a dataset-level `_attrs_cache` single source of truth, which also fixed deleted-variable attrs resurrecting at close and `attrs.clear()` never persisting. Pushing stays explicit-only (Mike's invariant): `sync()` writes only locally; nothing ever pushes on close. Original record: found 2026-07-02; the full `SysMeta` reached the booklet metadata slot only at close, so mid-session `push()` published a remote with no/stale variables and no attrs.
- [x] ~~**[cfdb] `open_edataset(flag='w')` with a fresh local file treats an EXISTING remote dataset as create-new**~~ — **RESOLVED in the 0.9.0 working tree 2026-07-03** (see the cfdb review+release item): `create` is now decided from remote existence too (`flag=='n'` always creates; otherwise create only if `get_metadata()` is None — it transparently checks the remote; verified that transparent pulls preserve remote timestamps so unchanged metadata never re-pushes). Drive-by: `EDataset.__init__` no longer rewrites unchanged metadata at open (previously EVERY write session pushed a spurious metadata change), and `open_edataset` now closes the ebooklet (releasing the write lock) if dataset construction fails. Original record: found 2026-07-02; `edataset.py:125` set `create=True` from local-file absence alone — the next push would destroy the remote dataset's structure metadata. envlib no longer needs the hydrate-first or close→reopen→push workarounds.
- [x] ~~**[cfdb] Commit + release the `ebooklet>=0.8.4` pin bumps**~~ — folded into the **cfdb 0.9.0 review + release** item above (pins now `>=0.9.0`, riding the same release).
- [x] ~~**[ebooklet] Review + release 0.9.1**~~ — **DONE 2026-07-05**: committed (`52f7c91`) and released to PyPI (0.9.1 wheel+sdist confirmed live); cfdb's `uv.lock` refreshed to ebooklet 0.9.1 (rides the pending cfdb 0.9.0 review). Delivered: the num_groups unpushed-reopen warning + the pin bumps (`s3func>=0.9.0`, `booklet>=0.12.4`) — the stale-s3func-resolution loop is now closed for downstream users. Verification record: 3 live regressions (warning tests fail pre-fix; re-pass path pushes grouped, verified by object listing; fresh creates and pushed-remote reopens warning-free); full suite 65/65; cfdb downstream 13/13; test-session cleanup hardened (loud + retry + verify-by-listing) after the 2026-07-03 killed-run leak.
- [x] ~~**[ebooklet] First push from a `'w'` reopen silently loses `num_groups`**~~ — **RESOLVED in the 0.9.1 working tree 2026-07-04** (see the ebooklet review+release item) as **loud-warning-only, no persistence** (Mike's decision 2026-07-04: minimize extra files to manage; "can add a file if it really becomes an issue"): a `'w'`/`'c'` reopen of an existing local file whose remote is uninitialized, without `num_groups`, emits a `UserWarning` (first push would go per-key); the supported path is re-passing the same `num_groups` until the first push, after which remote metadata takes over. Design history for the record: (1) the originally planned remote-index metadata slot proved unusable — booklet's base `set_metadata` on a `FixedLengthValue` corrupts fixed-stride iteration (see [booklet] item); (2) a uuid-bound sidecar (`<local>.ebooklet`) was implemented and fully verified, then replaced by the warning at Mike's preference. Original record: create `'n'`+num_groups → close without push → bare `'w'` reopen → first push silently went per-key.
- [x] ~~**[s3func] Review + release 0.9.1 — `delete_objects` response checking**~~ — **DONE 2026-07-05** (committed `25d5891`, PyPI 0.9.1 live). Original record: — implemented 2026-07-05 in the working tree (uncommitted, awaiting Mike's review): `parse_delete_errors` pure parser in `response.py` (namespace-aware, empty-body = success, malformed raises); `delete_objects` checks every chunk (non-2xx → whole chunk failed; 200 → per-key `<Error>` parse; unparseable 200 → chunk marked failed WITHOUT aborting later chunks) and raises `HTTPError` listing failed keys after attempting all chunks; docstring + CLAUDE.md contract notes; version 0.9.1. Verification: 8 unit tests (fail/won't-collect pre-fix), moto + fast local 41/41, live suite 69 passed/1 skipped (B2+MEGA), ebooklet downstream 65/65 + cfdb 13/13 against the patched tree (after fixing a uv `--with` stale-cache trap — see the session note in `s3func/CLAUDE.md`), bucket swept clean. Original record: `s3.py:481` fired the batched delete POST and ignored the response (urllib3 doesn't raise on status; Quiet=true still returns per-key `<Error>` elements) — failures silently no-oped; latent, found by inspection 2026-07-03.
- [x] ~~**[booklet] Review + release 0.12.5 — `FixedLengthValue.set_metadata` guard**~~ — **DONE 2026-07-05** (committed `376aee4`, PyPI 0.12.5 live). Original record: — implemented 2026-07-05 in the working tree (uncommitted, awaiting Mike's review): `set_metadata` on `FixedLengthValue` now raises `NotImplementedError` (the base-class write path uses variable-length framing that silently corrupts fixed-stride iteration); `get_metadata` stays inherited (returns None harmlessly); regression test verifies the raise AND that the file stays iterable; version 0.12.5. Suite 95/95; verified zero `set_metadata` callers on fixed booklets across the stack. Original record: found live 2026-07-03 (a metadata write into an ebooklet remote index broke `keys()` with garbage-key `UnicodeDecodeError`); proper fixed-file metadata support rejected as an unnecessary format change.
- [x] ~~**[s3func] Review + release 0.9.0**~~ — **DONE 2026-07-03**: committed (`ca27cc7`), released to PyPI (0.9.0 confirmed live). Bump ebooklet's `s3func>=0.9.0` pin at its next release. Original record: the instrumented race campaign reproduced the historical violations at scale on baseline code (**57 violations / 985 acquisitions, 5.8%**) and attributed **57/57 to decision bugs, 0 to listing staleness** (B2 exonerated): (1) the accumulation bug (older holder masked when a younger ticket iterates last — fires whenever ≥3 tickets coexist; this was the source of Mike's years of irreproducible flakes); (2) a second, subtler split-brain found by the first validation run (2/318): contenders judged their own age by PUT-response timestamps but competitors by listing timestamps — B2 lists at second granularity, so contention ties broke inconsistently. Fixed by the pure election core (`evaluate_election`: all-competitor scan, single-source timestamps from the listing, deterministic id tiebreak) + hardening (self-visibility gate, confirming re-list, own-ticket invariant, recovery re-election) + SigV4 fixes (path encoding for special-char keys AND strict RFC3986 query encoding — the latter found via MEGA, which doesn't canonicalize `+` as space) + B2-native deprecation + provider/session registry. **Final validation: 311 acquisitions, 0 violations, 0 errors** under the baseline-breaking conditions; 33 deterministic local tests incl. exhaustive small-model check; live suites green on B2+MEGA (13 special chars both providers); ebooklet downstream 62/62. New MEGA quirks documented (multi-delete hangs on `$` keys; `+`-in-query canonicalization) — reportable alongside the CAS atomicity issue. **Remaining close-out**: dual-blind review (nominated — this is lock code); Mike's source review + commit; PyPI release; ebooklet `s3func>=0.9.0` pin at next release.
- [x] ~~**[s3func] Object keys containing `!` fail B2 signature validation on put**~~ — **RESOLVED in the 0.9.0 working tree 2026-07-03** (see the s3func review+release item): root cause was the unencoded canonical URI in `signer.py`; fixed via `_object_url()` path encoding + strict RFC3986 query encoding; verified live with 13 special characters round-tripping on both B2 and MEGA. Ships with the 0.9.0 release. Original record: (observed 2026-07-02): per-key ebooklet uploads of cfdb chunk keys (e.g., `temperature!0.0.40`) all failed with `AccessDenied: Signature validation failed` — a URL-encoding mismatch in request signing for special-character keys. Invisible in normal cfdb use (grouped mode names objects by integer group id) and for RCGs (hex keys); bites any per-key-mode ebooklet with special-char keys. Verify against s3func signing code; fix or document a key-charset restriction.
- [x] ~~**[s3func] Fix bakery-lock weaknesses**~~ — **RESOLVED 2026-07-03** (see the 0.9.0 review+release item above: root-caused via instrumented campaign, fixed, validated 0-violations). Original record:
  1. *Accumulation bug* (by inspection, `s3func/locking.py:175-185`): `locked` is reassigned per iteration in `_check_for_older_objs`, so only the last-listed contender counts — with 3+ simultaneous writers, two can both acquire. One-line fix (`locked = locked or ...` or early-return).
  2. *LIST-after-PUT visibility race* (observed live): `acquire()` PUTs its lock objects then immediately LISTs for competitors; a competitor whose lock PUT hasn't propagated into LIST results yet is invisible, so two writers can both acquire even with only 2 contenders. Observed as a flaky lost-update in a threaded two-writer test against B2 (on ebooklet code with the 0.8.4 fixes — the lock itself failed to serialize). Mitigation shape: self-visibility check (poll LIST until your own lock objects appear, then evaluate competitors) + settle-delay re-list; reproducer: looped two-process acquire with mutual-exclusion assertion under parallel load. Corroboration: Mike's own s3func lock tests historically showed rare, irreproducible random failures — consistent with this window. Until fixed, truly-simultaneous first-contact writers to the same remote remain a small race window; ebooklet 0.8.4's post-lock metadata refresh protects every case where the lock actually serializes.
- [x] ~~**[s3func] Conditional-write (CAS) probe results across providers**~~ — **CLOSED 2026-07-03**: findings now recorded in-repo at `s3func/benchmarks/results_visibility_lag.md` (with the probe as the allowlist qualification gate). Original record: (2026-07-02, live probes; informs the lock design): **B2** rejects `If-None-Match` PUT with loud `NotImplemented` (S3-compat) and the native API has no conditional capability either (spec-checked). **MEGA S4** documents and honors `If-None-Match`/`If-Match` PUT *sequentially* (412 + body preserved) but is **NOT atomic under concurrency** — 8 simultaneous `If-None-Match:*` creates on one fresh key ALL returned 200 in 4 of 5 rounds (check-then-write, not CAS; probe script in session scratchpad, ready as a MEGA bug report reproducer). Design consequence: hardened bakery is the primary lock everywhere; a provider-allowlist CAS tier is cheap scaffolding but currently has zero qualifying members (AWS proper documents atomic conditional writes; unverified here). S4's conditional headers remain useful as non-atomic stale-write narrowing (e.g., If-Match on index pushes), never as a mutex. Also decided (Mike): deprecating s3func's native-B2 module is on the table pending the LIST-vs-native-version-list visibility measurement.
- [x] ~~**[booklet] `prune(timestamp)` type bug**~~ — **DONE 2026-07-02**: fixed (`6578a95`; the same-day `3d69511` was the separate 0.12.3 post-prune layout fix — attribution corrected 2026-07-09 while writing booklet's CHANGELOG), released to PyPI as 0.12.4 (confirmed live). Original record: the docstring promised int/str/datetime but `utils.prune_file` compared the raw value against int-microsecond timestamps — datetime/str raised `TypeError`. Fixed by normalizing via `utils.make_timestamp_int` in `Booklet.prune()` (None preserved — it means "no filter", not "now"); regression test covers datetime + ISO-string forms; suite 94/94 green. Sole outlier among booklet's timestamp-accepting entry points (audit: `set`/`set_metadata` convert in `write_data_blocks`; `_set_file_timestamp` converts explicitly). ebooklet 0.9.0's boundary workaround is harmless double-normalization — can be dropped in a future ebooklet tidy-up after this releases.
- [x] ~~**[ebooklet] Review + release 0.9.0**~~ — **DONE 2026-07-02**: committed (`1ee9955`) and released to PyPI (0.9.0 confirmed live). Optional post-implementation review round not run (Mike's call; the plan itself was Gemini-design-reviewed pre-implementation). Original scope for the record: (1) RCG step-0 enhancement — `add(conn, key=, user_meta=)`, entry schema v1 with write-time stamping (metadata-only upserts push), key-charset validation, `__setitem__`/`set` delegation, stale default-key cleanup; (2) `flag='n'`: loud num_groups guard + writes-only push contract (Mike's decision — pre-push reads stay transparent but never leak into the new db; dangling index entries eliminated); (3) `Change.pull()` repaired (full metadata reload, num_groups re-resolution, fetch-first index-handle cycling with finalizer re-registration); (4) prune contract pinned as local-eviction (docs + live test; timestamp normalized at the ebooklet boundary); (5) general lock-release-on-raise wrapper in `_init_common`. The plan was Gemini-design-reviewed pre-implementation (5 findings: 4 folded in; 1 proposed fix rejected as contract-breaking, its observation resolved by the writes-only decision). 62/62 suite green; envlib registration E2E (register→query→open member→metadata-only upsert→deregister) passed; booklet pin bumped to `>=0.12.3`. **Remaining close-out**: optional post-implementation Gemini review (nominated — the entry schema is envlib's never-change surface); Mike's source review + commit; PyPI release; envlib pins `ebooklet>=0.9.0` when its dependencies are added.
- [x] ~~**[ebooklet] Review + release 0.8.4**~~ — **DONE 2026-07-02**: push-integrity fixes implemented, Gemini-reviewed (2 review findings fixed, 2 deferred to design items — one remains in Backlog (clock-skew), 1 disputed, 1 mitigated), 8 regressions green, committed (`1e6f57d`) and released to PyPI (0.8.4 confirmed live). Conda status unverified (no `meta.yaml` found under `ebooklet/conda/`).
