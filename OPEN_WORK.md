# envlib — Open Work

Tracks **verification debts** (work done but not yet proven; each has a close-check — remove the item once it passes) and **backlog** (known work not yet started). The project spans repos — items are tagged: `[envlib]` (this repo), `[ebooklet]`, `[tethys]` (migration checks), `[infra]`.

_Last updated: 2026-07-02 (ebooklet 0.9.0 + booklet 0.12.4 RELEASED — remaining pre-envlib work: cfdb footguns + pin release, architecture-plan dual-blind review; s3func findings and deferred clock-skew open)_

## Verification debts

*(none — the three debts from the 2026-07-02 plan revision were all closed 2026-07-02: tethys interval-start timestamps confirmed at source + live public data; RCG concurrent-writer semantics verified safe-under-lock via live two-writer tests, with failure modes documented in the plan; pandas freqstr instability confirmed empirically against 2.1.4/2.2.3/3.0.3. Evidence in the plan's updated sections and the session report.)*

## Backlog

- [ ] **[cfdb] `EDataset.push()` inside a creation/modification session uploads a stale SysMeta** (found 2026-07-02): the full `SysMeta` (variables, shapes) reaches the booklet metadata slot only at close (`dataset_finalizer`, `utils.py:182-190`); `EDataset.__init__` writes an EMPTY SysMeta at open (`edataset.py:26`). A `ds.push()` before close therefore publishes a remote whose metadata has no/stale variables — readers see an empty or shape-stale dataset. The documented close-then-reopen-push workflow avoids it, but programmatic callers (envlib `cat.publish()`) will trip on it. Fix shape: `EDataset.push()` syncs the current `_sys_meta` via `set_metadata()` before `changes().push()`. Until then, envlib must implement close→reopen→push internally.
- [ ] **[cfdb] `open_edataset(flag='w')` with a fresh local file treats an EXISTING remote dataset as create-new** (found 2026-07-02 during the append smoke): `edataset.py:125` sets `create=True` from local-file absence alone; `Dataset.__init__` then builds an empty `SysMeta` (`main.py:762`) instead of loading the remote's (`main.py:776`), and `dataset_finalizer` (`utils.py:182-190`) writes that empty SysMeta over the real one on close — the next push would destroy the remote dataset's structure metadata. Fix shape: decide `create` from remote existence too (e.g., `flag == 'n'` or (local absent AND remote metadata absent)). Workaround until fixed: hydrate with a read open first, then reopen `'w'` — envlib's `cat.register()`/append flows must use this pattern.
- [x] ~~**[ebooklet] Review + release 0.8.4**~~ — **DONE 2026-07-02**: push-integrity fixes implemented, Gemini-reviewed (2 review findings fixed, 2 deferred to design items below, 1 disputed, 1 mitigated), 8 regressions green, committed (`1e6f57d`) and released to PyPI (0.8.4 confirmed live). Conda status unverified (no `meta.yaml` found under `ebooklet/conda/`).
- [ ] **[cfdb] Commit + release the `ebooklet>=0.8.4` pin bumps** — `cfdb/pyproject.toml` updated in the working tree 2026-07-02 (3 occurrences), uncommitted, awaiting Mike's review; takes effect for downstream users at the next cfdb release. envlib should also pin `ebooklet>=0.8.4` directly when its dependencies are added.
- [x] ~~**[ebooklet] Review + release 0.9.0**~~ — **DONE 2026-07-02**: committed (`1ee9955`) and released to PyPI (0.9.0 confirmed live). Optional post-implementation review round not run (Mike's call; the plan itself was Gemini-design-reviewed pre-implementation). Original scope for the record: (1) RCG step-0 enhancement — `add(conn, key=, user_meta=)`, entry schema v1 with write-time stamping (metadata-only upserts push), key-charset validation, `__setitem__`/`set` delegation, stale default-key cleanup; (2) `flag='n'`: loud num_groups guard + writes-only push contract (Mike's decision — pre-push reads stay transparent but never leak into the new db; dangling index entries eliminated); (3) `Change.pull()` repaired (full metadata reload, num_groups re-resolution, fetch-first index-handle cycling with finalizer re-registration); (4) prune contract pinned as local-eviction (docs + live test; timestamp normalized at the ebooklet boundary); (5) general lock-release-on-raise wrapper in `_init_common`. The plan was Gemini-design-reviewed pre-implementation (5 findings: 4 folded in; 1 proposed fix rejected as contract-breaking, its observation resolved by the writes-only decision). 62/62 suite green; envlib registration E2E (register→query→open member→metadata-only upsert→deregister) passed; booklet pin bumped to `>=0.12.3`. **Remaining close-out**: optional post-implementation Gemini review (nominated — the entry schema is envlib's never-change surface); Mike's source review + commit; PyPI release; envlib pins `ebooklet>=0.9.0` when its dependencies are added.
- [ ] **[ebooklet] Timestamp-based changelog silently ignores clock-skewed local edits** (Gemini review finding 6; **DEFERRED by decision 2026-07-02**): a local modification whose timestamp is OLDER than the remote's (clock skew or `set_timestamp`) never enters the changelog, so it never uploads — the 0.8.4 loud warning on the push-overwrite stands as the mitigation. A real fix needs modification tracking independent of timestamps (on-disk dirty flags in booklet). Low priority.
- [x] ~~**[booklet] `prune(timestamp)` type bug**~~ — **DONE 2026-07-02**: fixed (`3d69511`), released to PyPI as 0.12.4 (confirmed live). Original record: the docstring promised int/str/datetime but `utils.prune_file` compared the raw value against int-microsecond timestamps — datetime/str raised `TypeError`. Fixed by normalizing via `utils.make_timestamp_int` in `Booklet.prune()` (None preserved — it means "no filter", not "now"); regression test covers datetime + ISO-string forms; suite 94/94 green. Sole outlier among booklet's timestamp-accepting entry points (audit: `set`/`set_metadata` convert in `write_data_blocks`; `_set_file_timestamp` converts explicitly). ebooklet 0.9.0's boundary workaround is harmless double-normalization — can be dropped in a future ebooklet tidy-up after this releases.
- [ ] **[s3func] Object keys containing `!` fail B2 signature validation on put** (observed 2026-07-02): per-key ebooklet uploads of cfdb chunk keys (e.g., `temperature!0.0.40`) all failed with `AccessDenied: Signature validation failed` — a URL-encoding mismatch in request signing for special-character keys. Invisible in normal cfdb use (grouped mode names objects by integer group id) and for RCGs (hex keys); bites any per-key-mode ebooklet with special-char keys. Verify against s3func signing code; fix or document a key-charset restriction.
- [ ] **[s3func] Fix bakery-lock weaknesses** — two distinct facets, both verified 2026-07-02:
  1. *Accumulation bug* (by inspection, `s3func/locking.py:175-185`): `locked` is reassigned per iteration in `_check_for_older_objs`, so only the last-listed contender counts — with 3+ simultaneous writers, two can both acquire. One-line fix (`locked = locked or ...` or early-return).
  2. *LIST-after-PUT visibility race* (observed live): `acquire()` PUTs its lock objects then immediately LISTs for competitors; a competitor whose lock PUT hasn't propagated into LIST results yet is invisible, so two writers can both acquire even with only 2 contenders. Observed as a flaky lost-update in a threaded two-writer test against B2 (on ebooklet code with the 0.8.4 fixes — the lock itself failed to serialize). Mitigation shape: self-visibility check (poll LIST until your own lock objects appear, then evaluate competitors) + settle-delay re-list; reproducer: looped two-process acquire with mutual-exclusion assertion under parallel load. Corroboration: Mike's own s3func lock tests historically showed rare, irreproducible random failures — consistent with this window. Until fixed, truly-simultaneous first-contact writers to the same remote remain a small race window; ebooklet 0.8.4's post-lock metadata refresh protects every case where the lock actually serializes.

- [ ] **[envlib] Dual-blind independent review of the architecture plan** — before implementation step 0 begins. Claude subagent + Gemini (human-driven `agy`), same adversarial brief and scope for both; proposed scope: `envlib/plans/`, `ebooklet`, `cfdb-repos/cfdb`, `booklet`, `tethys-repos/tethys-utils` sources. The hash serialization rules are the highest-stakes target (permanent once a second party registers a dataset).
- [ ] **[envlib] Resolve flagged plan judgment calls** from the 2026-07-02 revision: hash-order position of `processing_level` (currently #5); `cat.deregister()` shape (one method + `delete_data` flag); `envlib_` attr prefix; `modified_at` bumps-only-on-change rule; placeholder frequency codes. Plus: the Metadata example's `product_code='era5'` + `owner='niwa'` contradicts the plan's own owner semantics — fix or justify.
- [ ] **[envlib] Finalize the frequency_interval code table** (implementation step 1) — closed set, one spelling per cadence, no anchoring variants.
- [ ] **[envlib] Curate the variable → CF standard_name mapping** for the v1 / tethys-migration variable subset — incomplete-by-design; `refresh()` reports upstream diffs but never regenerates the curated mapping.
- [ ] **[envlib] Implementation steps 1–5** per `plans/architecture_plan.md`: vocabularies → metadata → catalogue → tests → public API exports.
- [ ] **[infra] Public RCG hosting** — read-only public-HTTPS RCG on Backblaze (S3-compatible), `ENVLIB_PUBLIC_RCG_URL` env-var override.
